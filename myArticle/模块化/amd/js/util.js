define(function() {
    let utils = {
        add(x, y) {
            return x + y;
        },
        minus(x, y) {
            return x - y;
        },
        multiply(x, y) {
            return x * y;
        },
        divide(x, y) {
            return x / y;
        }
    }
    return utils;
});